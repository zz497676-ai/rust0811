# 文章可读性增强器 (Article Readability Formatter)

一个 Chrome 浏览器插件：一键调用 Claude API，为当前网页的长文章自动标注重点——
关键信息加粗、核心论点放大、次要信息缩小变淡、情绪明显的句子上色，让统一字号
的大段文字变得有层次、方便快速抓重点。

## 安装

1. 打开 `chrome://extensions`，右上角开启"开发者模式"。
2. 点击"加载已解压的扩展程序"，选择本仓库目录。
3. 点击插件图标 → 打开"设置" → 填写你的 Anthropic API Key（以及可选的模型 ID）。

## 使用

打开任意一篇长文章网页，点击插件图标，点"格式化本页文章"。插件会：

1. 在页面里找到正文容器（优先 `<article>`，否则用启发式规则找文本最集中的区块）。
2. 把正文按段落分块（每块约 3000 字符）交给 Claude 分析。
3. Claude 只在原文基础上插入标注标签，不改变任何文字，返回后由插件做白名单
   清洗（只允许 `<b>` 和固定几个 class 的 `<span>`），再写回页面对应段落。

页面右下角会显示处理进度。

## 效果对照

- **关键信息**：`<b>` 加粗
- **全文最核心的论点**：放大加粗（`arf-hi`）
- **次要/背景信息**：缩小变淡（`arf-lo`）
- **情绪偏正面**：绿色（`arf-pos`）
- **情绪偏负面/警示**：红色（`arf-neg`）

## 隐私与费用

- API Key 只保存在浏览器本地存储（`chrome.storage.local`），仅用于你的浏览器
  直接请求 `https://api.anthropic.com`，不会上传到任何其它服务器。
- 点击"格式化"后，当前页面的正文文本会被发送给 Anthropic 做分析，每次点击都会
  产生 API 调用费用，请留意用量。

## 已知限制

- 目前只处理 `<p>` 段落，且写回时会丢失段落内部原有的行内格式（链接、斜体等），
  统一替换成纯文本 + 标注结果。
- 正文识别是启发式规则，非典型排版的网页（大量 `<div>` 嵌套、无语义标签）可能
  识别不准。
- 模型返回内容依赖 prompt 约束"不改变原文用词"，理论上仍可能出现措辞漂移；如
  发现明显跑偏，欢迎反馈。
